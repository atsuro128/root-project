{
  "mcpServers": {
    "github": {
      "command": "/bin/bash",
      "args": [
        "-c",
        "GITHUB_PERSONAL_ACCESS_TOKEN=$(gh auth token) exec github-mcp-server stdio"
      ],
      "env": {
        "GITHUB_TOOLSETS": "actions,repos,issues,pull_requests"
      }
    }
  }
}