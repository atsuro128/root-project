# RBAC（ロールベースアクセス制御）要件

## 1. 概要

本書では、経費精算SaaS における RBAC の要件を定義する。
全APIエンドポイントに対してミドルウェアでロール検証を実施し、権限不足時は 403 Forbidden を返却する。

---

## 2. ロール定義

### 2.1 ロール一覧

| ロール | DB値 | 説明 | 付与タイミング |
|--------|------|------|--------------|
| **Admin** | `admin` | テナント管理者 | テナント作成時に自動付与 / Admin が手動付与 |
| **Approver** | `approver` | 経費承認者 | Admin が付与 |
| **Member** | `member` | 一般社員（申請者） | Admin が付与（招待時のデフォルト） |
| **Accounting** | `accounting` | 経理担当 | Admin が付与 |

### 2.2 ロールの責務

```
Admin ─────── テナント全体の管理・監視
                ├── メンバー招待（Phase 3）
                ├── ロール管理（Phase 3）
                ├── 全レポート閲覧
                └── テナント設定

Approver ───── 経費の承認判断
                ├── 承認待ちレポートの閲覧
                ├── 承認
                ├── 却下（理由必須）
                └── 自分の経費申請（Member としての操作）

Member ──────── 経費の申請
                ├── レポート作成・編集・削除（自分のもののみ）
                ├── 明細追加・編集・削除
                ├── 領収書添付・削除
                ├── レポート提出
                └── ステータス確認

Accounting ──── 経費の支払処理
                ├── 承認済みレポートの確認
                ├── 支払完了の記録
                └── 経費一覧の閲覧
```

### 2.3 ロールの関係

- ロールは**排他的**: 1ユーザーは1テナントにつき1つのロールのみ持つ
- Approver は Member の操作も行える（自分の経費を申請可能）
- Admin は自分が作成したレポートに限り、申請者として作成・編集・削除・提出が可能である
- Admin は他者のレポートについては閲覧のみ可能であり、編集・承認・却下はできない
- MVP では Approver は同一テナント内の submitted 状態のレポートをすべて閲覧・承認・却下できる（全件承認型）。担当者割当や承認経路の個別設定は Phase 3 以降の拡張とする

---

## 3. 権限マトリクス

### 3.1 認証関連

| API操作 | 未認証 | Admin | Approver | Member | Accounting |
|---------|--------|-------|----------|--------|------------|
| サインアップ | ○ | - | - | - | - |
| ログイン | ○ | - | - | - | - |
| トークンリフレッシュ | ○ | - | - | - | - |
| ログアウト | - | ○ | ○ | ○ | ○ |
| 自分の情報取得 | - | ○ | ○ | ○ | ○ |

### 3.2 経費レポート

| API操作 | Admin | Approver | Member | Accounting | 補足 |
|---------|-------|----------|--------|------------|------|
| レポート作成 | ○ | ○ | ○ | × | Approver/Admin も自分の経費を申請可能 |
| 自分のレポート一覧 | ○ | ○ | ○ | × | |
| 自分のレポート詳細 | ○ | ○ | ○ | × | |
| テナント全体のレポート一覧 | ○ | × | × | ○ | Admin: 管理目的、Accounting: 支払管理目的 |
| レポート編集（draft） | ※ | ※ | ※ | × | ※ 自分のレポートのみ |
| レポート削除（draft） | ※ | ※ | ※ | × | ※ 自分のレポートのみ |
| レポート提出 | ※ | ※ | ※ | × | ※ 自分のレポートのみ |

### 3.3 承認フロー

| API操作 | Admin | Approver | Member | Accounting | 補足 |
|---------|-------|----------|--------|------------|------|
| 承認待ち一覧 | × | ○ | × | × | 同テナントの submitted レポート全件（MVP は全件承認型） |
| 承認 | × | ○ | × | × | 自己承認は禁止 |
| 却下 | × | ○ | × | × | 却下理由必須 |
| 支払待ち一覧 | × | × | × | ○ | 同テナントの approved レポート |
| 支払完了 | × | × | × | ○ | |

### 3.4 明細・添付

| API操作 | Admin | Approver | Member | Accounting | 補足 |
|---------|-------|----------|--------|------------|------|
| 明細追加 | ※ | ※ | ※ | × | ※ 自分のdraftレポートのみ |
| 明細編集 | ※ | ※ | ※ | × | ※ 自分のdraftレポートのみ |
| 明細削除 | ※ | ※ | ※ | × | ※ 自分のdraftレポートのみ |
| 明細閲覧 | ○ | △ | ※ | ○ | △ 承認対象のレポートのみ |
| 添付アップロード | ※ | ※ | ※ | × | ※ 自分のdraftレポートのみ |
| 添付削除 | ※ | ※ | ※ | × | ※ 自分のdraftレポートのみ |
| 添付ダウンロード | ○ | △ | ※ | ○ | △ 承認対象のレポートのみ |

### 3.5 管理機能

| API操作 | Admin | Approver | Member | Accounting | 補足 |
|---------|-------|----------|--------|------------|------|
| テナント情報閲覧 | ○ | × | × | × | |
| メンバー一覧（Phase 3） | ○ | × | × | × | |
| メンバー招待（Phase 3） | ○ | × | × | × | |
| ロール変更（Phase 3） | ○ | × | × | × | |
| メンバー削除（Phase 3） | ○ | × | × | × | |
| 監査ログ閲覧（Phase 3） | ○ | × | × | × | |
| CSVエクスポート（Phase 3） | ○ | × | × | ○ | |

**凡例**:
- ○ = 許可
- × = 禁止
- ※ = 所有者であることが条件（自分が作成したリソースのみ操作可能）
- △ = 関連業務対象であることが条件（例: Approver は承認対象のレポートのみ、Accounting は支払対象のレポートのみ）

---

## 4. アクセス制御のルール

### 4.1 基本原則

| ID | ルール | 実装場所 |
|----|--------|---------|
| RBC-001 | 全APIでミドルウェアによるロール検証 | 認可ミドルウェア |
| RBC-002 | 1ユーザー = 1テナント = 1ロール | tenant_memberships テーブル |
| RBC-003 | 「ロール」+「所有権」の二重チェック | ハンドラ or ドメイン層 |
| RBC-004 | 権限不足時は 403 Forbidden | 認可ミドルウェア |

### 4.2 所有権ルール

| ID | ルール | 対象操作 |
|----|--------|---------|
| RBC-010 | 申請系操作（作成/編集/削除/提出）は、ロール（Member/Approver/Admin）に関わらず所有者のレポートに限定 | CRUD、提出 |
| RBC-011 | Approver は同テナントの submitted レポートを承認/却下可能 | 承認、却下 |
| RBC-012 | Accounting は同テナントの approved レポートに支払完了を記録可能 | 支払完了 |
| RBC-013 | Admin は同テナントの全レポートを閲覧可能（編集不可） | 閲覧 |

### 4.3 特殊ルール

| ルール | 内容 | 根拠 |
|--------|------|------|
| **自己承認禁止** | Approver が自分のレポートを承認/却下できない | 内部統制 |
| **Admin の二面性** | Admin は自分が作成したレポートに限り申請者として操作可能（作成・編集・削除・提出）。他者のレポートは閲覧のみで、編集・承認・却下はできない | 所有権の尊重・不正改ざん防止 |
| **Approver の Member 操作** | Approver は自分自身の経費レポートを作成・編集・提出できる（Member と同等の操作） | 実務上、承認者も経費を立て替える |

---

## 5. テナント分離との関係

RBAC はテナント分離と組み合わせて動作する。

```
リクエスト受信
    ↓
① 認証チェック（JWT 検証）
    → 失敗: 401 Unauthorized
    ↓
② テナント特定（JWT から tenant_id を取得）
    ↓
③ ロールチェック（ミドルウェア）
    → 権限不足: 403 Forbidden
    ↓
④ 所有権チェック（ハンドラ or ドメイン層）
    → 他人のリソース: 403 Forbidden
    ↓
⑤ テナント分離（リポジトリ層で tenant_id フィルタ）
    → 他テナントのデータは一切取得不可
    ↓
⑥ RLS（DB層で二重保証）
    → 万が一アプリ層が漏れても DB が防御
```

### 認可チェックの層別責務

| 層 | 責務 | 例 |
|----|------|-----|
| **ミドルウェア** | ロールの存在チェック | 「この API は Approver 以上のみ」 |
| **ハンドラ / ドメイン層** | 所有権・ビジネスルール | 「自分のレポートか？」「自己承認でないか？」 |
| **リポジトリ層** | テナント分離 | WHERE tenant_id = ? の強制 |
| **DB（RLS）** | テナント分離の二重保証 | アプリ層が漏れた場合の防御 |

---

## 6. エラーレスポンス

### 認可エラーの統一形式

| HTTP ステータス | 状況 | レスポンス例 |
|---------------|------|-------------|
| 401 Unauthorized | 未認証（トークンなし/無効/期限切れ） | `{"error": "unauthorized", "message": "Authentication required"}` |
| 403 Forbidden | ロール不足 | `{"error": "forbidden", "message": "Insufficient permissions"}` |
| 403 Forbidden | 所有権不足 | `{"error": "forbidden", "message": "Access denied"}` |
| 404 Not Found | 他テナントのリソース | `{"error": "not_found", "message": "Resource not found"}` |

> **セキュリティ上の注意**: 他テナントのリソースにアクセスした場合は 403 ではなく **404** を返す。403 はリソースの存在を漏洩するため、テナント境界を超えたアクセスでは「存在しない」として扱う。

---

## 7. MVP でのロール管理

### 初期ロール付与

MVP ではメンバー招待機能（Phase 3）がないため、以下の方式でロールを管理する。

| 方法 | 説明 |
|------|------|
| サインアップ | テナント作成者に Admin ロールを自動付与 |
| シードデータ | 開発・デモ用にシードスクリプトで各ロールのユーザーを作成 |
| DB直接操作 | 必要に応じて DB で tenant_memberships のロールを変更（開発時のみ） |

> Phase 3 で招待フロー（Admin がメール + ロール指定で招待）を実装する。

### デモ・レビュー用テストアカウント（シードデータ）

MVP ではメンバー招待機能がないため、シードスクリプトで以下のユーザーを作成する。

| ユーザー | ロール | メール（例） | 用途 |
|---------|--------|-------------|------|
| 管理者 太郎 | Admin | admin@example.com | テナント管理、全体閲覧 |
| 承認者 花子 | Approver | approver@example.com | 承認・却下操作、自分の経費申請 |
| 一般 次郎 | Member | member@example.com | 経費レポート作成・提出 |
| 経理 美咲 | Accounting | accounting@example.com | 支払処理 |

- 全ユーザーは同一テナント（デモ企業）に所属
- パスワードは共通の開発用パスワード（本番では使用禁止）
- シードデータにはサンプルの経費レポート（各状態）も含める

---

## 8. 品質チェック

- [x] 全 API に対してロール別の許可/禁止が定義されているか
- [x] 所有権チェックのルールが明確か
- [x] テナント分離との組み合わせが矛盾していないか
- [x] 自己承認禁止のルールが明記されているか
- [x] エラーレスポンスが統一されているか
- [x] 他テナントリソースへのアクセスで 404 を返す（存在漏洩防止）
